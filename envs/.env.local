# 로컬 환경 — Kafka docker-compose + 로컬 파일시스템 싱크
# 이 파일만 git 추적 (.env* 전부 gitignore, 이 파일만 !.env.local 예외)

KAFKA_BOOTSTRAP=localhost:9092
KAFKA_SECURITY_PROTOCOL=PLAINTEXT
KAFKA_TOPIC=drone-telemetry

SINK_PATH=data/streaming/features
CHECKPOINT_PATH=data/streaming/checkpoint